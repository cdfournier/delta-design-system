import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './buttons.stories';

<Meta of={ComponentStories} title="Atoms/Buttons/Documentation" />

# Buttons

Buttons trigger actions or navigate users through interfaces. They communicate interactive affordance through visual weight, color, and feedback states.

## Anatomy

Buttons consist of:

- **Label:** Action-oriented text describing what the button does.
- **Container:** Background and border that define the clickable area and communicate variant/style.
- **Icon (optional):** Visual reinforcement placed before (left) or after (right) the label.

<Canvas of={ComponentStories.AnatomyFigure} />

## Variants

Buttons have two axes of variation: **variant** (color palette) and **style** (visual treatment).

### Variant

- **Primary (default):** Uses the brand red palette. For the most important action on a page. Limit to one primary button per section to maintain clear hierarchy.
- **Secondary:** Uses the brand teal palette. For supporting actions that complement the primary action without competing for attention.
- **White:** Uses white as the foreground color. For buttons placed on dark or colored backgrounds where primary and secondary colors would lack contrast.

<Canvas of={ComponentStories.VariantsFigure} />

### Style

Style controls the visual weight and fill treatment of buttons. Regular buttons support two styles: solid and transparent.

Regular text buttons support:

- **Solid (default):** Filled background for strongest emphasis.
- **Transparent:** Transparent background with a transparent border in the default state. On hover, the background becomes solid and the border becomes visible in the variant color. The lowest visual weight. Use for low-emphasis actions, inline controls, or contexts where minimal visual presence is required.

<Canvas of={ComponentStories.StyleFigure} />

### Icon-only buttons

Icon-only buttons display a single icon without a text label, providing a compact interactive element for toolbars, navigation, and space-constrained interfaces. Icon buttons support all three variants (`primary`, `secondary`, `white`) and all three styles (`solid`, `outline`, `transparent`).

Icon buttons use a circular border-radius (`border-radius/round`) and uniform padding on all sides. Because there is no visible label, icon-only buttons require accessible labeling. Use either a visually hidden `<span>` element or the `aria-label` attribute.

The outline style is available only for icon-only buttons. It provides a middle weight between solid and transparent.

<Canvas of={ComponentStories.IconOnlyFigure} />

## States

Button states provide feedback about interaction and availability.

### Default

Resting state before user interaction.

### Hover

Regular buttons invert their appearance on hover: solid buttons swap to a transparent background with a visible border, and transparent buttons gain a solid filled background. Icon-only outline buttons invert between outline and solid fill.

<Canvas of={ComponentStories.HoverFigure} />

### Focus

Focus uses browser default focus ring. Do not suppress default focus behavior.

### Disabled

Disabled buttons use the `status/disabled` token (`#808080`) for both background and border, with white text. Transparent style buttons use `#808080` for text only, with no fill or border. Include both the `disabled` HTML attribute and `aria-disabled="true"` to ensure proper accessibility behavior.

<Canvas of={ComponentStories.DisabledFigure} />

## Typography

Button text uses Montserrat Bold at a fixed size with a 1:1 line-height ratio.

<table>
  <thead>
    <tr>
      <th>Property</th>
      <th>Token</th>
      <th>Mobile/Tablet</th>
      <th>Widescreen</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>font-size</td>
      <td><code>components/button/font-size</code></td>
      <td>0.75rem / 12px</td>
      <td>1.5rem / 24px</td>
    </tr>
    <tr>
      <td>line-height</td>
      <td><code>components/button/line-height</code></td>
      <td>0.75rem / 12px</td>
      <td>1.5rem / 24px</td>
    </tr>
  </tbody>
</table>

## Spacing

Button padding scales responsively using global spacing tokens. At widescreen, horizontal padding doubles to maintain visual balance with larger layouts.

<table>
  <thead>
    <tr>
      <th>Property</th>
      <th>Token</th>
      <th>Mobile/Tablet</th>
      <th>Widescreen</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>padding-top, padding-bottom</td>
      <td><code>components/button/padding-t-b</code> → <code>md</code></td>
      <td>0.5rem / 8px</td>
      <td>1rem / 16px</td>
    </tr>
    <tr>
      <td>padding-left, padding-right</td>
      <td><code>components/button/padding-l-r</code> → <code>xl</code></td>
      <td>1.5rem / 24px</td>
      <td>3rem / 48px</td>
    </tr>
    <tr>
      <td>gap</td>
      <td><code>components/button/padding-gap</code> → <code>sm</code></td>
      <td>0.25rem / 4px</td>
      <td>0.5rem / 8px</td>
    </tr>
    <tr>
      <td>Icon-only padding</td>
      <td><code>sm</code></td>
      <td>0.25rem / 4px</td>
      <td>0.5rem / 8px</td>
    </tr>
  </tbody>
</table>

For spacing between buttons in a group, see the Placement subsection under Usage.

## Border-radius

Buttons use a fixed border-radius token that does not scale responsively. The full-round shape is defined by the `button/border-radius` token.

<table>
  <thead>
    <tr>
      <th>Element</th>
      <th>Token</th>
      <th>Mobile/Tablet</th>
      <th>Widescreen</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Button container</td>
      <td><code>components/button/border-radius</code> → <code>button/border-radius</code></td>
      <td>2rem / 32px (fixed)</td>
      <td>2rem / 32px (fixed)</td>
    </tr>
    <tr>
      <td>Icon-only button</td>
      <td><code>border-radius/round</code></td>
      <td>6.25rem / 100px (fixed)</td>
      <td>6.25rem / 100px (fixed)</td>
    </tr>
  </tbody>
</table>

## Colors

Button colors vary by variant and state. The disabled state uses a shared token across all variants and styles. Buttons are mode-invariant; the same color values are used in both light and dark mode.

<table>
  <thead>
    <tr>
      <th>Element / State</th>
      <th>Token</th>
      <th>Light mode</th>
      <th>Dark mode</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Primary background (default)</td>
      <td><code>brand/primary</code></td>
      <td>#FF5247</td>
      <td>#FF5247</td>
    </tr>
    <tr>
      <td>Primary background (hover)</td>
      <td><code>global/white</code></td>
      <td>#FFFFFF</td>
      <td>#FFFFFF</td>
    </tr>
    <tr>
      <td>Primary text / border</td>
      <td><code>brand/primary</code></td>
      <td>#FF5247</td>
      <td>#FF5247</td>
    </tr>
    <tr>
      <td>Secondary background (default)</td>
      <td><code>brand/secondary</code></td>
      <td>#003442</td>
      <td>#003442</td>
    </tr>
    <tr>
      <td>Secondary background (hover)</td>
      <td><code>global/white</code></td>
      <td>#FFFFFF</td>
      <td>#FFFFFF</td>
    </tr>
    <tr>
      <td>Secondary text / border</td>
      <td><code>brand/secondary</code></td>
      <td>#003442</td>
      <td>#003442</td>
    </tr>
    <tr>
      <td>White background (default)</td>
      <td><code>global/white</code></td>
      <td>#FFFFFF</td>
      <td>#FFFFFF</td>
    </tr>
    <tr>
      <td>White text / border</td>
      <td><code>global/white</code></td>
      <td>#FFFFFF</td>
      <td>#FFFFFF</td>
    </tr>
    <tr>
      <td>White hover background</td>
      <td><code>brand/primary</code></td>
      <td>#FF5247</td>
      <td>#FF5247</td>
    </tr>
    <tr>
      <td>Disabled background / border</td>
      <td><code>status/disabled</code></td>
      <td>#808080</td>
      <td>#808080</td>
    </tr>
    <tr>
      <td>Disabled text (transparent style)</td>
      <td><code>status/disabled</code></td>
      <td>#808080</td>
      <td>#808080</td>
    </tr>
    <tr>
      <td>Label text on filled backgrounds</td>
      <td><code>global/white</code></td>
      <td>#FFFFFF</td>
      <td>#FFFFFF</td>
    </tr>
  </tbody>
</table>

**Light/Dark mode behavior:** Button colors are mode-invariant. All variant and state colors resolve to the same values in both light and dark mode.

## Accessibility

### Keyboard navigation

All buttons must be fully keyboard accessible:

- **Tab:** Moves focus to the next button.
- **Shift + Tab:** Moves focus to the previous button.
- **Enter:** Activates the focused button.
- **Space:** Activates the focused button (native `<button>` elements only).

### Screen readers

Use semantic `<button>` elements for actions and `<a>` elements for navigation. Native `<button>` elements are announced correctly without additional ARIA. For icon-only buttons, use a visually hidden `<span>` rather than `aria-label`; the hidden span approach is more robust for translation tools and voice control software.

### ARIA attributes

- **`aria-disabled="true"`:** Apply alongside the `disabled` HTML attribute on disabled buttons to ensure both visual and programmatic disabled state are communicated.
- **`aria-label`:** Use only if no visible label is present and a visually hidden span is not feasible.
- **`aria-pressed`:** Use on toggle buttons that switch between on/off states.
- **`aria-expanded`:** Use on buttons that open or close a region (for example: accordions, dropdowns).

### Focus management

Do not suppress or override the browser default focus ring. The default focus outline provides keyboard users with reliable visual feedback across browsers and operating systems. Where custom focus styles are needed, ensure they meet WCAG 2.1 AA requirements: a minimum `3:1` contrast ratio for the focus indicator against adjacent colors.

### Color contrast

All button text must meet WCAG AA contrast requirements (`4.5:1` for normal text). Primary and secondary variants on solid and outline styles achieve this. White buttons on dark backgrounds are designed with sufficient contrast in mind; verify contrast when placing white buttons on non-system background colors. Disabled buttons intentionally use reduced contrast (`status/disabled`) to visually communicate unavailability; this is an accepted WCAG exception for disabled components.

## Usage

### When to use

- **Primary actions:** Main actions a user should take (submit, save, create, confirm).
- **Secondary actions:** Supporting or alternative actions (cancel, go back, skip).
- **Form submission:** Triggering form validation and submission.
- **Navigation triggers:** Opening modals, expanding sections, or switching views.

### When not to use

- **Navigation to new pages:** Use a Link component for navigating between pages or to external URLs.
- **Inline text emphasis:** Do not use buttons to draw attention to inline text.
- **Non-interactive elements:** Buttons must always trigger an action; never use them as decorative or static elements.

### Content guidelines

- **Lead with action verbs:** Start labels with clear verbs (for example: "Save changes," "Delete item," "Create account").
- **Be specific:** Avoid generic labels like "Click here," "Submit," or "OK" without contextual meaning.
- **Keep text short:** Button labels should be 1-3 words; four words maximum in rare cases.
- **Use sentence case:** Capitalize only the first word unless the label contains a proper noun.

### Placement

Button placement should follow the natural reading flow and content alignment of the surrounding context.

- **Right-aligned groups:** In forms and modals (the most common pattern), place the primary action on the right and secondary actions to its left.
- **Left-aligned groups:** When buttons align with left-aligned content, place the primary action on the left.
- **Mobile stacking:** On small screens, buttons in a group typically stack vertically and span the full container width; place the primary action on top.
- **Spacing between buttons:** Use `0.5rem / 8px (md)` between buttons in a group on mobile/tablet, and `1rem / 16px (md)` at widescreen.
- **Destructive actions:** Add extra visual separation between destructive actions (delete, remove) and non-destructive ones to prevent accidental activation.

### Best practices

<Story of={ComponentStories.BestPracticesFigure} />

## Code reference

### HTML

```html
<!-- Use <button> for actions -->
<button type="button" class="btn btn-primary-solid">
  Save changes
</button>

<!-- Use <a> for navigation -->
<a href="/page" class="btn btn-secondary-solid">View details</a>

<!-- Transparent style -->
<button type="button" class="btn btn-secondary-transparent">Learn more</button>

<!-- Disabled button -->
<button type="button" class="btn btn-primary-solid" disabled aria-disabled="true">Submit</button>

<!-- Button with icon (left) -->
<button type="button" class="btn btn-primary-solid">
  <svg aria-hidden="true" focusable="false" width="16" height="16">...</svg>
  Save changes
</button>

<!-- Icon-only button with aria-label -->
<button type="button" class="btn btn-primary-solid btn-icon" aria-label="Settings">
  <svg aria-hidden="true" focusable="false" width="16" height="16">...</svg>
</button>

<!-- Icon-only button with visually-hidden label (preferred) -->
<button type="button" class="btn btn-primary-solid btn-icon">
  <span class="visually-hidden">Settings</span>
  <svg aria-hidden="true" focusable="false" width="16" height="16">...</svg>
</button>

<!-- Icon-only button with outline style -->
<button type="button" class="btn btn-primary-outline btn-icon" aria-label="Menu">
  <svg aria-hidden="true" focusable="false" width="16" height="16">...</svg>
</button>
```

### CSS

```css
/* Visually-hidden utility */
.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

/* Base button (mobile/tablet) */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.25rem;
  padding: 0.5rem 1.5rem;
  border-radius: 2rem;
  font-size: 0.75rem;
  line-height: 0.75rem;
  font-weight: 700;
  font-family: var(--font-family);
  text-decoration: none;
  border: 1px solid transparent;
  cursor: pointer;
  transition: background-color 0.15s ease, color 0.15s ease, border-color 0.15s ease;
}

/* Base button (widescreen) */
@media (min-width: 80rem) {
  .btn {
    gap: 0.5rem;
    padding: 1rem 3rem;
  }
}

/* Icon-only button */
.btn-icon {
  padding: 0.25rem;
  width: 1.5rem;
  height: 1.5rem;
  border-radius: 6.25rem;
}

@media (min-width: 80rem) {
  .btn-icon {
    padding: 0.5rem;
    width: 3rem;
    height: 3rem;
  }
}

/* Primary solid */
.btn-primary-solid {
  background-color: var(--brand-primary, #FF5247);
  color: #FFFFFF;
  border-color: var(--brand-primary, #FF5247);
}
.btn-primary-solid:hover,
.btn-primary-solid:focus-visible {
  background-color: #FFFFFF;
  color: var(--brand-primary, #FF5247);
  border-color: var(--brand-primary, #FF5247);
}

/* Primary outline */
.btn-primary-outline {
  background-color: transparent;
  color: var(--brand-primary, #FF5247);
  border-color: var(--brand-primary, #FF5247);
}
.btn-primary-outline:hover,
.btn-primary-outline:focus-visible {
  background-color: var(--brand-primary, #FF5247);
  color: #FFFFFF;
  border-color: var(--brand-primary, #FF5247);
}

/* Primary transparent */
.btn-primary-transparent {
  background-color: transparent;
  color: var(--brand-primary, #FF5247);
  border-color: transparent;
}
.btn-primary-transparent:hover,
.btn-primary-transparent:focus-visible {
  color: var(--brand-primary-dark, #B22E28);
}

/* Secondary solid */
.btn-secondary-solid {
  background-color: var(--brand-secondary, #003442);
  color: #FFFFFF;
  border-color: var(--brand-secondary, #003442);
}
.btn-secondary-solid:hover,
.btn-secondary-solid:focus-visible {
  background-color: #FFFFFF;
  color: var(--brand-secondary, #003442);
  border-color: var(--brand-secondary, #003442);
}

/* Secondary outline */
.btn-secondary-outline {
  background-color: transparent;
  color: var(--brand-secondary, #003442);
  border-color: var(--brand-secondary, #003442);
}
.btn-secondary-outline:hover,
.btn-secondary-outline:focus-visible {
  background-color: var(--brand-secondary, #003442);
  color: #FFFFFF;
  border-color: var(--brand-secondary, #003442);
}

/* Secondary transparent */
.btn-secondary-transparent {
  background-color: transparent;
  color: var(--brand-secondary, #003442);
  border-color: transparent;
}
.btn-secondary-transparent:hover,
.btn-secondary-transparent:focus-visible {
  color: var(--brand-secondary-dark, #01232D);
}

/* White variants (for dark backgrounds) */
.btn-white-solid {
  background-color: #FFFFFF;
  color: var(--brand-primary, #FF5247);
  border-color: #FFFFFF;
}
.btn-white-solid:hover,
.btn-white-solid:focus-visible {
  background-color: var(--brand-primary, #FF5247);
  color: #FFFFFF;
  border-color: #FFFFFF;
}

.btn-white-outline {
  background-color: transparent;
  color: #FFFFFF;
  border-color: #FFFFFF;
}
.btn-white-outline:hover,
.btn-white-outline:focus-visible {
  background-color: #FFFFFF;
  color: var(--brand-primary, #FF5247);
  border-color: #FFFFFF;
}

.btn-white-transparent {
  background-color: transparent;
  color: #FFFFFF;
  border-color: transparent;
}
.btn-white-transparent:hover,
.btn-white-transparent:focus-visible {
  color: #CCCCCC;
}

/* Disabled */
.btn:disabled,
.btn[aria-disabled="true"] {
  background-color: var(--status-disabled, #808080);
  color: #FFFFFF;
  border-color: var(--status-disabled, #808080);
  cursor: not-allowed;
}

.btn-primary-transparent:disabled,
.btn-primary-transparent[aria-disabled="true"],
.btn-secondary-transparent:disabled,
.btn-secondary-transparent[aria-disabled="true"],
.btn-white-transparent:disabled,
.btn-white-transparent[aria-disabled="true"] {
  background-color: transparent;
  color: var(--status-disabled, #808080);
  border-color: transparent;
}

.btn-icon.btn-primary-outline:disabled,
.btn-icon.btn-primary-outline[aria-disabled="true"],
.btn-icon.btn-secondary-outline:disabled,
.btn-icon.btn-secondary-outline[aria-disabled="true"],
.btn-icon.btn-white-outline:disabled,
.btn-icon.btn-white-outline[aria-disabled="true"] {
  background-color: transparent;
  color: var(--status-disabled, #808080);
  border-color: var(--status-disabled, #808080);
}
```

### Design tokens reference

```text
/* Typography */
--components/button/font-size      Mobile: 0.75rem / 12px   ->   Widescreen: 1.5rem / 24px
--components/button/line-height    Mobile: 0.75rem / 12px   ->   Widescreen: 1.5rem / 24px

/* Spacing */
--components/button/padding-t-b    Mobile: 0.5rem / 8px     ->   Widescreen: 1rem / 16px
--components/button/padding-l-r    Mobile: 1.5rem / 24px    ->   Widescreen: 3rem / 48px
--components/button/padding-gap    Mobile: 0.25rem / 4px    ->   Widescreen: 0.5rem / 8px
--components/button/icon-padding   Mobile: 0.25rem / 4px    ->   Widescreen: 0.5rem / 8px

/* Sizing */
--components/button/icon-button-size   Mobile: 1.5rem / 24px   ->   Widescreen: 3rem / 48px
--components/button/icon-size          Mobile: 1rem / 16px     ->   Widescreen: 2rem / 32px

/* Border radius */
--components/button/border-radius      2rem / 32px (fixed)
--border-radius/round                  6.25rem / 100px (fixed)

/* Colors */
--brand/primary                #FF5247
--brand/primary-dark           #B22E28
--brand/secondary              #003442
--brand/secondary-dark         #01232D
--global/white                 #FFFFFF
--global/gray-medium           #CCCCCC
--status/disabled              #808080
```

## Resources

- [ARIA Authoring Practices: Button Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/button/)
- [WebAIM](https://webaim.org/)
- [WCAG 2.1 Quick Reference](https://www.w3.org/WAI/WCAG21/quickref/)
- [MDN: button element](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button)
- [WCAG 2.1: Understanding Target Size](https://www.w3.org/WAI/WCAG21/Understanding/target-size.html)
- [Nielsen Norman Group: Button States](https://www.nngroup.com/articles/button-states-communicate-interaction/)

## Source of truth

- [Figma documentation node](https://www.figma.com/design/tHT8Je5cvRTXFN9XQ7XqhA/The-Delta-Design-System?node-id=1073-3066&m=dev)
- Figma Variables table and modes define all values.
- HTML export may support copy/structure but must be verified against Figma.
